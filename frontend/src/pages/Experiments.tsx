import { Card } from "../components/Card";

// Placeholder -- full implementation lands in Phase 5 (Personalization
// Experiment page: subject selection, preset demo, histogram + cross-
// application results).
export default function Experiments() {
  return (
    <div className="space-y-6">
      <div>
        <h1 className="text-2xl font-semibold tracking-tight">Personalization experiment</h1>
        <p className="mt-1 text-sm text-muted">Coming shortly.</p>
      </div>
      <Card>
        <p className="text-sm text-muted">This page is being built.</p>
      </Card>
    </div>
  );
}
